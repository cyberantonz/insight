replicaCount: 1

podAnnotations: {}

image:
  repository: ghcr.io/constructorfabric/insight-analytics
  tag: ""                  # MUST be set by the operator. No `:latest` default.
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8081

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

# Pre-provisioned Secret carrying the gears-rust host's leaf-config overrides
# (injected via envFrom; they override the mounted config ConfigMap):
#   APP__gears__analytics__config__database_url
#   APP__gears__analytics__config__clickhouse_url|_database|_user|_password
#   APP__gears__analytics__config__redis_url
#   APP__gears__analytics__config__identity_url
#   APP__gears__analytics__config__metric_catalog__tenant_default_id (optional)
#
# `..._metric_catalog__tenant_default_id` (UUID, optional): single-tenant
# fallback for the Metric Catalog per DESIGN §2.2
# `cpt-metric-cat-constraint-tenant-default`. Under the auth-disabled host the
# platform api-gateway is the sole authenticator and this host injects a
# single-tenant SecurityContext (toolkit-security's DEFAULT_TENANT_ID); the
# analytics gear overrides the tenant from the `X-Insight-Tenant-Id`
# header when present. The session-bound tenant ALWAYS wins over the
# configured default (security invariant; see
# `domain::auth::TenantAuthorization`).
#
# The umbrella populates `insight-analytics-config` automatically; for
# standalone installs the operator pre-creates an equivalent Secret and
# sets `existingSecret` to its name.
existingSecret: ""

# Health probes
livenessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 5
  periodSeconds: 5
